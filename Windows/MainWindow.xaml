<Window
    x:Class="justQuit.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{Binding WindowTitle}"
    Width="900"
    Height="760"
    MinWidth="760"
    MinHeight="690"
    WindowStartupLocation="CenterScreen">
    <Grid Background="#FFF6F1E7">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Margin="18">
            <TabItem Header="Apps">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Left">
                                <TextBlock FontSize="30" FontWeight="Bold" Text="justQuit" />
                                <TextBlock Margin="0,6,0,0" Foreground="#775A43" TextWrapping="Wrap"
                                           Text="Quit regular apps by default, and choose whether background apps stay skipped or get included too." />
                            </StackPanel>
                            <StackPanel DockPanel.Dock="Right" Margin="16,0,0,0" VerticalAlignment="Top">
                                <Border Padding="10,6" Background="#FFE6E0D4" CornerRadius="16">
                                    <TextBlock Text="{Binding VersionBadgeText}" />
                                </Border>
                                <Border Margin="0,8,0,0" Padding="12,7" Background="#FF2563EB" CornerRadius="16" HorizontalAlignment="Right">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsProBadgeVisible}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Foreground="White" FontWeight="Bold" Text="{Binding ProBadgeText}" />
                                </Border>
                            </StackPanel>
                        </DockPanel>

                        <WrapPanel Margin="0,12,0,16">
                            <Border Margin="0,0,10,10" Padding="10,6" Background="#FFF1D9B5" CornerRadius="16">
                                <TextBlock Text="{Binding AppsToQuitCount, StringFormat=Eligible now: {0} app(s)}" />
                            </Border>
                            <Border Margin="0,0,10,10" Padding="10,6" Background="#FFD5E8D4" CornerRadius="16">
                                <TextBlock Text="{Binding ProtectedRegularAppCount, StringFormat=Protected: {0}}" />
                            </Border>
                            <Border Margin="0,0,10,10" Padding="10,6" Background="#FFE6E0D4" CornerRadius="16">
                                <TextBlock Text="{Binding SkippedBackgroundAppCount, StringFormat=Background skipped: {0}}" />
                            </Border>
                            <Border Margin="0,0,10,10" Padding="10,6" Background="#FFD5E8D4" CornerRadius="16">
                                <TextBlock Text="{Binding IncludedBackgroundAppCount, StringFormat=Background included: {0}}" />
                            </Border>
                        </WrapPanel>

                        <DockPanel Margin="0,0,0,12">
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <Button Width="165" Margin="0,0,10,0" Padding="14,8" Click="QuitAllClicked" Content="Quit All Eligible Apps" />
                                <Button Width="90" Margin="0,0,10,0" Padding="14,8" Click="RefreshClicked" Content="Refresh" />
                                <Button Width="145" Padding="14,8" Click="RestoreClicked" Content="Restore Last Session" />
                            </StackPanel>
                            <TextBlock Width="280" HorizontalAlignment="Right" TextAlignment="Right" VerticalAlignment="Center"
                                       Foreground="#775A43" TextWrapping="Wrap" Text="{Binding StatusMessage}" />
                        </DockPanel>

                        <TextBox Margin="0,0,0,16" Padding="10,8" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

                        <GroupBox Header="Apps You Can Protect From Quitting" Margin="0,0,0,16">
                            <ItemsControl Margin="10" ItemsSource="{Binding RegularApps}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,10" Padding="12" Background="White" CornerRadius="12" BorderBrush="#FFE3D7C7" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Orientation="Horizontal">
                                                    <Image Width="24" Height="24" Source="{Binding Icon}" Stretch="Uniform" />
                                                    <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" Text="{Binding Name}" />
                                                </StackPanel>
                                                <Button Grid.Column="1" Padding="12,6" Click="ProtectionButtonClicked" Content="{Binding ActionText}" IsEnabled="{Binding CanToggle}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </GroupBox>

                        <GroupBox Header="Background Apps">
                            <ItemsControl Margin="10" ItemsSource="{Binding BackgroundApps}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,10" Padding="12" Background="White" CornerRadius="12" BorderBrush="#FFE3D7C7" BorderThickness="1">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Orientation="Horizontal">
                                                    <Image Width="24" Height="24" Source="{Binding Icon}" Stretch="Uniform" />
                                                    <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold" Text="{Binding Name}" />
                                                </StackPanel>
                                                <Button Grid.Column="1" Padding="12,6" Click="ProtectionButtonClicked" Content="{Binding ActionText}" IsEnabled="{Binding CanToggle}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Settings">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="18">
                        <GroupBox Header="App Settings" Margin="0,0,0,18">
                            <StackPanel Margin="12">
                                <CheckBox Margin="0,0,0,8" IsChecked="{Binding LaunchAtLoginEnabled}" Content="Launch at login" />
                                <CheckBox Margin="0,0,0,8" IsChecked="{Binding NotificationsEnabled}" Content="Enable notifications" />
                                <CheckBox Margin="0,0,0,8" IsChecked="{Binding HotkeyEnabled}" Content="Enable global hotkey (Ctrl+Alt+J)" />
                                <CheckBox Margin="0,0,0,8" IsChecked="{Binding ConfirmLargeQuitsEnabled}" IsEnabled="{Binding IsProUnlocked}" Content="Confirm when quitting many apps" />
                                <CheckBox Margin="0,0,0,8" IsChecked="{Binding CountdownEnabled}" IsEnabled="{Binding IsProUnlocked}" Content="Enable countdown before quitting" />
                                <TextBlock Margin="0,8,0,4" Text="{Binding ConfirmationThreshold, StringFormat=Confirm when quitting {0}+ apps}" />
                                <Slider Minimum="1" Maximum="50" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding ConfirmationThreshold}" IsEnabled="{Binding IsProUnlocked}" />
                                <TextBlock Margin="0,12,0,4" Text="{Binding CountdownSeconds, StringFormat=Countdown seconds: {0}}" />
                                <Slider Minimum="1" Maximum="30" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding CountdownSeconds}" IsEnabled="{Binding IsProUnlocked}" />
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="justQuit Pro" Margin="0,0,0,18">
                            <StackPanel Margin="12">
                                <TextBlock Foreground="#775A43" Text="{Binding LicenseStatusMessage}" TextWrapping="Wrap" />
                                <TextBlock Margin="0,8,0,0" FontWeight="SemiBold" Text="{Binding LicenseIdText}" />
                                <DockPanel Margin="0,12,0,0">
                                    <Button DockPanel.Dock="Right" Width="130" Margin="10,0,0,0" Click="LicenseActionClicked" Content="{Binding LicenseActionButtonText}" />
                                    <TextBox Padding="10,8" Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsLicenseKeyEditable}" />
                                </DockPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Recent Session Restore" Margin="0,0,0,18">
                            <StackPanel Margin="12">
                                <TextBlock Foreground="#775A43" Text="{Binding LastRestoreSummaryText}" TextWrapping="Wrap" />
                                <Button Width="140" Margin="0,12,0,0" Click="RestoreClicked" Content="Restore Last Session" />
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Updates" Margin="0,0,0,18">
                            <StackPanel Margin="12">
                                <TextBlock Margin="0,0,0,10" Foreground="#775A43" Text="{Binding VersionBadgeText}" />
                                <StackPanel Orientation="Horizontal">
                                    <Button Width="140" Click="CheckForUpdatesClicked" Content="Check for Updates" />
                                    <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Foreground="#775A43" Text="{Binding UpdateStatusText}" />
                                </StackPanel>
                                <TextBlock Margin="0,12,0,0" FontWeight="SemiBold" Text="{Binding AvailableUpdateVersionText}" />
                                <TextBlock Margin="0,4,0,0" Foreground="#775A43" Text="{Binding AvailableUpdateSizeText}" />
                                <TextBlock Margin="0,4,0,0" Foreground="#775A43" Text="{Binding AvailableUpdateNotesText}" TextWrapping="Wrap" />
                                <Button Width="110" Margin="0,12,0,0" Click="InstallUpdateClicked" Content="Update Now" />
                                <TextBlock Margin="0,8,0,0" Foreground="#775A43" Text="{Binding UpdateErrorMessage}" TextWrapping="Wrap" />
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Backup &amp; Transfer">
                            <StackPanel Margin="12" Orientation="Horizontal">
                                <Button Width="120" Margin="0,0,10,0" Click="ExportSettingsClicked" Content="Export Settings" />
                                <Button Width="120" Click="ImportSettingsClicked" Content="Import Settings" />
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Profiles">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="18">
                        <GroupBox Header="Profiles">
                            <StackPanel Margin="12">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBox Width="240" Margin="0,0,10,0" Padding="10,8" Text="{Binding NewProfileName, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Width="120" Click="SaveProfileClicked" Content="Save Current" IsEnabled="{Binding IsProUnlocked}" />
                                </StackPanel>
                                <ItemsControl ItemsSource="{Binding Profiles}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,0,10" Padding="12" Background="White" CornerRadius="12" BorderBrush="#FFE3D7C7" BorderThickness="1">
                                                <DockPanel>
                                                    <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding Name}" />
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                        <Button Width="70" Margin="0,0,10,0" Click="ApplyProfileClicked" Content="Apply" IsEnabled="{Binding DataContext.IsProUnlocked, RelativeSource={RelativeSource AncestorType=Window}}" />
                                                        <Button Width="70" Click="DeleteProfileClicked" Content="Delete" IsEnabled="{Binding DataContext.IsProUnlocked, RelativeSource={RelativeSource AncestorType=Window}}" />
                                                    </StackPanel>
                                                </DockPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <TextBlock Grid.Row="1" Margin="0,0,0,14" HorizontalAlignment="Center" Foreground="#775A43" FontWeight="SemiBold" Text="{Binding FooterText}" />
    </Grid>
</Window>
